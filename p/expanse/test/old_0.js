

    //! Remove?
/* cb = null;
p = {...playerHit};
p.y += player.vy - 1;
// p.h -= 10;
X: for (x = minx; x < maxx; x++) {
  for (y = miny; y < maxy; y++) {
    if (
      data.collide.includes(grid[x][y].block)
      && !data.walkInto.includes(grid[x][y].block)
    ) {
      if (F.collide(p, {
        x: (x + 0.001) * tw,
        y: (y + 0.001) * tw,
        w: tw + 1,
        h: tw + 1,
      })) {
        cb = grid[x][y];
        break X;
      }
    }
  }
}
if (cb) {
  // player.vy = 0;
  // player.y -= 1.1;
} */